import React from 'react';

const Loading = () => {
    return (
        <div className={`border-2 border-dashed rounded-full mx-4 w-6 h-6 animate-spin border-blue-600`}>
        </div>
    );
};

export default Loading;